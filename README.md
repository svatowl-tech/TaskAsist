# ⚡ TaskAssist PWA

**TaskAssist** — это современный, работающий в офлайн-режиме (Offline-first) персональный менеджер задач и ассистент, построенный как Progressive Web App (PWA).

Приложение объединяет классические методики продуктивности (Kanban, Календарь, Pomodoro) с возможностями генеративного ИИ (как облачного, так и локального через WebGPU), обеспечивая приватность и гибкость.

![React](https://img.shields.io/badge/React-19-blue?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5.5-blue?logo=typescript)
![Vite](https://img.shields.io/badge/Vite-5.4-purple?logo=vite)
![TailwindCSS](https://img.shields.io/badge/Tailwind-3.4-38bdf8?logo=tailwindcss)
![PWA](https://img.shields.io/badge/PWA-Supported-purple)
![License](https://img.shields.io/badge/License-MIT-green)

---

## ✨ Ключевые возможности

### 🛠 Управление задачами
*   **Гибкие представления:** Переключение между Списком, Канбан-доской и Календарем.
*   **Умный ввод:** Волшебная кнопка (Magic Input) для создания проектов и задач одной фразой через ИИ.
*   **Повторяющиеся задачи:** Настройка ежедневных, еженедельных или ежемесячных повторений.
*   **Pomodoro таймер:** Встроенный таймер фокуса с интеграцией в лог времени задачи.

### 🧠 Искусственный Интеллект (Copilot)
*   **Dual Mode:**
    *   ☁️ **Cloud:** Поддержка OpenRouter (DeepSeek, GPT-4, Gemini) для сложных задач.
    *   🏠 **Local:** Запуск LLM (Llama-3, TinyLlama) прямо в браузере через WebGPU (полная приватность).
*   **Функции:** Генерация чеклистов, улучшение текста заметок, создание планов проектов, анализ загруженности.
*   **Чат-ассистент:** Мультимодальный чат (текст + изображения) для помощи в планировании.

### 🔒 Данные и Синхронизация
*   **Offline-first:** Все данные хранятся локально в **IndexedDB**. Приложение работает без интернета.
*   **Синхронизация:**
    *   Поддержка **Google Drive** (App Data Folder).
    *   Поддержка **GitHub Gist**.
*   **E2EE Шифрование:** Возможность зашифровать данные паролем перед отправкой в облако (AES-GCM).
*   **Бэкапы:** Локальные снапшоты и экспорт в JSON/CSV/ICS.

### 🎨 UX/UI
*   **PWA:** Установка на рабочий стол (iOS/Android/Desktop), работа в отдельном окне.
*   **Темы:** Поддержка светлой и темной темы.
*   **Жесты:** Swipe-действия для задач (выполнено/удалить) на мобильных устройствах.

---

## 🚀 Быстрый старт

### Требования
*   Node.js 18+
*   Браузер с поддержкой современных Web API (для WebGPU требуется Chrome/Edge/Arc).

### Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/username/task-assist.git
    cd task-assist
    ```

2.  **Установите зависимости:**
    ```bash
    npm install
    ```

3.  **Настройка переменных окружения (Опционально):**
    Создайте файл `.env` в корне проекта для настройки OAuth (если планируете разрабатывать авторизацию):
    ```env
    VITE_GOOGLE_CLIENT_ID=ваш_google_client_id
    VITE_YANDEX_CLIENT_ID=ваш_yandex_client_id
    ```

4.  **Запуск в режиме разработки:**
    ```bash
    npm run dev
    ```
    Приложение будет доступно по адресу `http://localhost:3000`.

5.  **Сборка для продакшена:**
    ```bash
    npm run build
    ```

---

## 🏗 Архитектура

Приложение построено на **React 19** с использованием функциональной архитектуры:

*   **State Management:** Кастомный легковесный Store (`lib/store.ts`) с подписками, работающий в связке с IndexedDB.
*   **Services Layer:** Вся бизнес-логика вынесена в сервисы (`services/`):
    *   `StorageService`: Обертка над IndexedDB.
    *   `SyncService`: Логика слияния данных и работа с API облаков.
    *   `AiService`: Единый интерфейс для Cloud и Local AI.
    *   `AutomationService`: Обработка правил автоматизации и триггеров.
*   **Performance:**
    *   Использование `Web Workers` для тяжелых вычислений (аналитика).
    *   Ленивая загрузка компонентов (`React.lazy`).
    *   Виртуализация списков (в планах).

---

## 🛠 Технологический стек

*   **Frontend Framework:** React 19
*   **Build Tool:** Vite
*   **Language:** TypeScript
*   **Styling:** Tailwind CSS
*   **Database:** IndexedDB (Native API)
*   **AI Inference:** Web-LLM (MLC AI) для WebGPU
*   **Testing:** Кастомный Test Runner (`lib/testRunner.ts`)

---

## 📱 PWA Возможности

Приложение полностью соответствует критериям PWA:
*   Работает офлайн (Service Worker с кэшированием).
*   Manifest файл для установки.
*   Обработка `share_target` для получения контента из других приложений.
*   Поддержка `protocol_handlers` (схемы `web+taskassist://`).

---

## 🤝 Контрибьютинг

Мы приветствуем любой вклад!

1.  Форкните репозиторий.
2.  Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`).
3.  Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`).
4.  Запушьте ветку (`git push origin feature/AmazingFeature`).
5.  Откройте Pull Request.

Для тестирования используйте встроенный Test Runner, доступный по url `/?mode=test`.

---

## 📄 Лицензия

Распространяется под лицензией MIT. Подробнее см. файл `LICENSE`.

---

<div align="center">
  <sub>Built with ❤️ and ☕ using React & AI</sub>
</div>
